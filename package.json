{
  "name": "syle-github-io",
  "version": "1.0.17",
  "private": true,
  "description": "Sy's resume along with some mini apps",
  "main": "index.html",
  "scripts": {
    "build": "rm -f *.html *.css && node generate-index-html.js && npx --yes -p sass sass src/index.scss index.css && node generate-resume.js",
    "start": "npx http-server --cors . -p 3000"
  },
  "keywords": [
    "Sy",
    "Le"
  ],
  "author": "Sy Le",
  "license": "ISC",
  "devDependencies": {
    "mustache": "^4.2.0",
    "puppeteer": "^24.4.0"
  }
}
